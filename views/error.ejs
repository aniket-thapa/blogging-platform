<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error</title>
    <link rel="stylesheet" href="/public/css/tailwind.css" />
  </head>
  <body
    class="bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-gray-100 min-h-screen flex flex-col"
  >
    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center px-4">
      <div
        class="max-w-lg w-full bg-white dark:bg-gray-800 shadow-2xl rounded-xl p-10 text-center"
      >
        <!-- Icon -->
        <div class="flex justify-center mb-6">
          <svg
            class="w-16 h-16 text-red-600 dark:text-red-500"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M12 9v2m0 4h.01M21 12A9 9 0 1 1 3 12a9 9 0 0 1 18 0z"
            />
          </svg>
        </div>

        <!-- Heading -->
        <h1
          class="text-3xl md:text-4xl font-bold mb-4 text-red-600 dark:text-red-500"
        >
          Something Went Wrong
        </h1>

        <!-- Message -->
        <% if (typeof message !== 'undefined' && message) { %>
        <p class="text-md md:text-lg text-gray-600 dark:text-gray-300 mb-6">
          <%= message %>
        </p>
        <% } else { %>
        <p class="text-md md:text-lg text-gray-600 dark:text-gray-300 mb-6">
          An unexpected error occurred. Please try again later.
        </p>
        <% } %>

        <!-- Button -->
        <a
          href="/"
          class="inline-block bg-blue-600 dark:bg-blue-500 hover:bg-blue-700 dark:hover:bg-blue-600 text-white font-medium px-6 py-3 rounded-lg transition-all duration-200"
        >
          Return Home
        </a>
      </div>
    </main>
  </body>
</html>
